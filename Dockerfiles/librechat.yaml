version: 1.3.8
registration:
  open: true

endpoints:
  custom:
    - name: "LIA Local"
      apiKey: "not-used"
      baseURL: "http://model-loader:3002/v1"
      models:
        default:
          - "lia-local"
        fetch: true
      titleConvo: true
      titleModel: "lia-local"